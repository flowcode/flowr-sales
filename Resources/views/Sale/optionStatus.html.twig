{% for status in options_statuses %}

    <form action="{{ path('sale_change_status', {'id':sale.id}) }}" method="post" class="form-inline">
        <input value="{{ status.id }}" type="hidden" name="status_to">
        <button type="submit" class="btn btn-default btn-sm">
            {{ status.name }}
        </button>
    </form>

{% endfor %}