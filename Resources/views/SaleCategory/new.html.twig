{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% creation'|trans({'%entity%': 'SaleCategory'}, 'SaleCategory') }} - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        {% include 'FlowerCoreBundle:Default:admin_nav.html.twig' %}
        <h1>
            <a href="{{ path('admin_salecategory') }}"> {{ '%entity% List'|trans({'%entity%': 'SaleCategory'}, 'SaleCategory') }}</a>
            /
            {{ 'SaleCategory' | trans({}, 'SaleCategory')}}
        </h1>

    </section>        
    <section class="content">
        <div class="box">
            <div class="box-body">
                {{ form_start(form, {action: path('admin_salecategory_create')}) }}
                    {{ form_widget(form) }}
                    <p>
                        <button class="btn-default btn" type="submit">{{ 'save'|trans | capitalize}}</button>
                    </p>
                {{ form_end(form) }}
            </div>
            <div class="box-footer">
                        
        <div class="btn-group record_actions"></div>

            </div>
        </div>
    </section>
{% endblock %}
