<section class="sidebar">
    <div class="app-header">
        <div class="row">
            <div class="col-sm-12">
                <img src="{{ asset('bundles/flowercore/images/icons/PurchaseOrder-100.png') }}"
                     class="img-responsive pull-left" width="40">
                <span>{{ '%entity% list'|trans({'%entity%': 'Sale'}, 'Sale') }}</span>
            </div>
        </div>
    </div>
    <ul class="sidebar-menu">

        <li {% if app_sale is defined %}class="active"{% endif %}>
            <a href="{{ path('sale') }}">{{ '%entity% list'|trans({'%entity%': 'Sale'}, 'Sale') }}</a>
        </li>
        <li {% if app_sale_report is defined %}class="active"{% endif %}>
            <a href="{{ path('sale_report') }}">{{ 'Stadistics'|trans({}, 'Sale') }}</a>
        </li>

    </ul>
</section>